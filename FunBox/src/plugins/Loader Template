// loader-example.js
export default async function(container, src, behavior) {
  // Load your animation or asset
  const plugin = await loadYourAsset(src);

  // Mount to the container
  container.appendChild(plugin.domElement);

  // Return control interface
  return {
    play: (state) => {
      plugin.setState(state);
    }
  };
}
